import React from "react";
import style from "../../../css_modules/addPost.module.css";

const RemoveBox = (props) => (
    <div className={`${style.imageRemoveBox} col-3`}>
    </div>
);

export default RemoveBox;